# Local development (host Postgres)
DATABASE_URL=postgresql+psycopg://USER:PASS@HOST:5432/DBNAME

POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=

APP_ENV=dev
LOG_LEVEL=INFO

# NOTE: In Docker, the API service overrides DATABASE_URL to:
# postgresql+asyncpg://library:library@db:5432/library
